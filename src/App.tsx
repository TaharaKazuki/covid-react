import React, { useEffect, useState } from 'react'

const App = () => {
  const 
  useEffect(() => {
    const data = async () => {
      const data = await fetchData()
    }

    data()
  
  })
}

export default App
